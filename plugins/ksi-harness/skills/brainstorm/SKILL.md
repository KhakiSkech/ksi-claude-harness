---
name: brainstorm
description: 문제나 설계에 대해 다양한 접근을 발산시키고, 군집화한 뒤, trade-off를 비교해 추천안으로 수렴한다. 방향이 안 잡혔거나 선택지를 넓게 보고 싶을 때 쓴다.
when_to_use: 설계 초기, 접근법이 여러 개일 때, 또는 "어떻게 풀지 모르겠다" 싶을 때.
---

# Brainstorm — 발산 → 수렴

## 절차

1. **문제 정의** — 무엇을 풀려는지, 제약과 성공 기준을 한 줄로 고정한다.
2. **발산** — 서로 다른 각도에서 최소 3~5개 접근을 낸다.
   - 예: MVP-우선 / 리스크-우선 / 사용자-우선 / 단순함-우선 / 비관습적 한 방
   - 작업이 크면 **workflow로 각 각도를 병렬 에이전트에 fan-out** 한다 (ultracode 친화). 발산·조사 워커는 `agent({model: 'haiku'})`/`'sonnet'`(또는 scout/worker)로 내리고, 수렴·trade-off 판정은 메인이 한다 — 모델 미지정 시 메인을 inherit하므로 alias로 명시.
3. **군집화** — 비슷한 아이디어를 묶고 중복을 제거한다.
4. **비교** — 표로 trade-off 정리: 장점 / 단점 / 비용 / 리스크 / 적합 상황.
5. **수렴** — 1개 추천 + 근거. 차순위 안에서 살릴 아이디어가 있으면 접목한다.
6. **다음 단계** — 추천안을 실행하려면 무엇부터인지 제시.

## 원칙

- 발산 단계에선 비판 금지 — 일단 넓게. 수렴 단계에서 냉정하게 친다.
- 추천은 분명하게. **"둘 다 좋다"로 끝내지 않는다.**
